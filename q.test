PUT -> "/comments/:commentId/like-status":
 создать комментарий,
 затем: отрицательная оценка комментария пользователем 1 и пользователем 2;
 положительная оценка комментария пользователем 3;
 получить комментарий после каждой положительной оценки от пользователя 1; статус 204;

 использованы дополнительные методы: POST => /blogs, POST => /posts, POST => /posts/:postId/comments, GET => /comments/:id;