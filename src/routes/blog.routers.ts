import {Request, Response, Router} from "express";

const blogRouters = Router({})

blogRouters.get('/',(req:Request,res:Response)=>{

})
blogRouters.get('/:id',(req:Request,res:Response)=>{})
blogRouters.post('/:id',(req:Request,res:Response)=>{})
blogRouters.put('/:id',(req:Request,res:Response)=>{})
blogRouters.delete('/:id',(req:Request,res:Response)=>{})